# `@city/stylelint-config-sass`

### Подключение пакета

```sh
npm install --save-dev stylelint@13 @city/stylelint-config-sass
```

Создать файл `.stylelintrc.json` и скопировать в него следующие правила

```json
{
  "extends": ["@city/stylelint-config-sass"]
}
```
